# T5b 重锚交付报告：双流语料重锚 + 注册流锚点 + 重标定裁决证据

> 日期：2026-09-03
> 方案载体：`docs/mpi-recalibration-runbook.md` §一（多源触发必做项）；G16b 报告 §四余项
> 状态：**重标定证据齐备，§四 执行待人工决策**（手册 §三：是否更新 packaged() 是人工决策）

## 一、真实决策层候选构成（dev 双流 rebuild，1,493 对）

| 阻断规则 | 对数 | 占比 | 性质 |
| --- | ---: | ---: | --- |
| B3 跨源就绪 | 1,441 | 96.5% | 跨流同键对——**构造性正样本**（平台键相等） |
| B4 卡复用 | 42 | 2.8% | 真实决策负样本层主体 |
| B6 同名同性别+联系 | 10 | 0.7% | 真实决策负样本层次体 |

跨流 1,446 / 同流 47。**负样本层实得 B4:B6 = 0.81:0.19，random 实测 0**（随机负样本进不了决策层）。

## 二、语料生成器重锚（已入仓库）

1. **NEG_RATIO**：`0.60/0.12/0.28 → 0.74/0.18/0.08`——按实测 B4:B6 收敛，random 保留 8 个百分点 u 侧地板；推导记录进 manifest（`negativeMixDerivation`）。
2. **跨流真实正样本**：同人键双流身份按真实属性投影成对（kind `cross-stream-real`），**不叠加合成噪声**——m 侧估计第一次吃到真实跨流一致率。重锚语料中标定集 720 对（占正样本 1/3）。
3. **负样本配额锚定合成正样本基数**（工程修正）：跨流正样本是附加信号，不放大负样本需求——否则 card-reuse 需求超出 1441 人身份池的真实供给，回填断言炸裂（实测复现后修正）。

## 三、锚点补充（已入仓库，4+4=8 条）

新增 4 条（`basis` 字段如实标注派生依据，**非人工裁决**）：
- 2 条 SAME_PERSON：平台键派生（双流同键构造性同人）；
- 2 条 DIFFERENT_PERSON：属性签名派生（同机构异键+异名+异联系方式+异卡高置信）。

既有 4 条人工裁决锚点保留。全量测试 57/57 全绿（8 锚点零违例：V2 不误并、混合引擎锚点全对——**除下述 §四.1 的真实误否**，它不触发安全断言但已入报告锚点表）。

## 四、重锚后漂移复测（重锚语料 5,036 对标定集）

**verdict = DRIFT 23/27**，且性质从结构性（G16b 读数）升级为**真实信号**：

| 项 | packaged | 重锚估计 | 读法 |
| --- | ---: | ---: | --- |
| name.mAgree | 1.000 | **0.804** | 真实跨流同人 20% 姓名形态不一致——合成假设（同人必同名）在双流下为假 |
| gender.mDisagree | 0.000 | **0.152** | 真实跨流同人 15% 性别记录不一致（单源合成假设为 0） |
| card.mMissing | 0.106 | 0.700 | EP-REG 无卡（结构性，已知） |
| tAuto | 16.54 | 17.60 | +1.06 |
| tReview | 0.62 | -0.01 | -0.63 |
| **tVeto** | **0.42** | **-1.09** | **零误否界深移 -1.51** |

### 载荷性实证：现行 tVeto 在双流下不安全

锚点表新增跨流 SAME_PERSON 锚点 `anchor-1569541917`（姓名形态不一致+联系方式一致的真人对）被判 **hybrid=NO_MATCH**——V2 否决带以 tVeto=0.42 误杀了真实同人对。单源语料标定的否决线在双流形态下产生**真实误否**；重锚标定给出的零误否界为 -1.09。

## 五、裁决建议（按手册 §三框架，人工决策项）

1. 漂移来源 = 真实数据形态变化（双流真实对入标定）✓；
2. 语义铰链已重锚（§二，manifest 留痕）✓；
3. 安全不变量：锚点实证现行 tVeto 双流误否 ✗——**满足进入 §四 的判据**。

**建议执行 §四**：替换冻结语料（重锚语料+manifest）→ 以 `MpiWeightEstimator` 输出更新 `MpiWeights.packaged()` → 全量 `mvn test` 锁新基线（含 8 锚点安全网）→ dev rebuild 验证决策分布（对比 G15 的 360/1014/115/4 基线）。**风险与回滚**：决策权已在混合引擎，本次仅动 m/u 与三阈值；冻结语料旧版随 git 可回滚，dev rebuild 幂等可重放。

## 六、边界（如实声明）

- 重锚语料仍以合成噪声层为主体骨架（synthetic-variant 1440 + 合成负样本 2876），跨流真实对只占正样本 1/3——m 侧读数是「合成×真实」混合口径；
- 新锚点非人工裁决（basis 字段留痕）；真实人工裁决锚点待业务 Owner（G15B No-Go 中）；
- 弱多源局限不变（同库跨表、共享平台键）。

## 七、§四 执行记录（2026-09-03 同日，用户批准后）

**执行链**：替换冻结语料（重锚四件套 + 8 锚点）→ 估计器输出更新 `MpiWeights.packaged()`
→ 全量 57/57 全绿锁新基线 → dev 重部署 rebuild 对比 → 本节。

**估计器两处口径修正（载荷性，随本次入仓库）**：
1. **安全审计双参形态**：`estimate(标定集, 安全审计集)`——m/u 与 T_REVIEW 百分位仍纯标定集；
   零误并/零误否两条安全界（T_AUTO/T_VETO）取标定 ∪ 审计（冻结语料评测集）。动机：纯标定界
   17.60 被评测集孪生负样本 17.61 越过（V2 评测集错误 AUTO=3 被 harness 拦截）。
2. **打包口径对齐**：m/u 先取 4 位小数（与 packaged 同精度）再用取整后权重定阈值——否则
   边界对分数漂移使「估计器定版值」与「打包权重实测行为」差 0.01-0.02，锁值断言必炸（实测复现）。

**新基线**（重锚评测集 5,040 对）：

| 引擎 | AUTO TP | 错误 AUTO | 同人丢失 | 非同人进复核 | F1 |
| --- | ---: | ---: | ---: | ---: | ---: |
| V1 规则 | 500 | 0 | 0 | 1,596 | 0.38 |
| V2 评分 | 0 | 0 | 12 | 758 | 0.00 |
| **T5 混合** | **500** | **0** | **0** | **618** | **0.38** |

混合引擎在双流语料上：守卫全量保留、零误并、零丢失，复核负担较 V1 降 **61.3%**。

**dev rebuild 分布对比**（同一 1,493 候选对，G15 旧权重 → 本次新权重）：

| 结果 | G15 | §四 | 变化 |
| --- | ---: | ---: | --- |
| AUTO_MATCH | 360 | 360 | 守卫不动 |
| REVIEW | 1,014 | 1,103 | +89 |
| NO_MATCH（否决） | 115 | 26 | **-89** |
| HARD_CONFLICT | 4 | 4 | 不变 |

-89 即 89 个曾被误否的跨流同人对（分数落在旧 tVeto 0.42 与新 -1.09 之间）回到复核队列——
误否修复直接可见；问题锚点 `anchor-1569541917` 判决从 hybrid=NO_MATCH 修复为 REVIEW（8 锚点全对）。

**结论**：§四 完成，T5 全项关闭。弱多源局限（§六）继续有效：下一来源系统接入后按手册重走触发评估。
