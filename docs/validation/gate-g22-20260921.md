# G22 数据标准中心真实化 — 验收记录

> 日期：2026-09-21
> 对照清单：[feature-completion-acceptance-checklists-20260920.md](../feature-completion-acceptance-checklists-20260920.md) §G22 + §0
> 提交：ab59a02（22 文件；control-plane 238/238、门户全链绿、CI run 35551955796 success）
> 结论：**通过**（代码/测试/CI + dev 真实链路证据，2026-09-21 实测；dev 证据附于文末）。

## 验收对照

| 清单项 | 结论 | 证据 |
| --- | --- | --- |
| G22-1 Flyway V17 五表 | ✅ | `V17__standard.sql`：data_standard（租户内 code 唯一）/ data_standard_version（版本号单调唯一、状态机、sync_status）/ data_standard_element（类型白名单约束在服务层、版本内 code 唯一）/ data_standard_value（元素内 code 唯一）/ data_standard_event（不可变审计）。H2 测试同套迁移重放通过 |
| G22-2 12 接口 + 同步重试 | ✅ | `StandardController`：GET/POST /data-standards、GET /{id}、POST /{id}/versions、PUT /data-standard-versions/{id}、submit/publish/deprecate、import（CSV/JSON 双格式）、compare/{other}、impact、fhir-bundle，另加 POST .../sync（SYNC_PENDING 人工重试闭环，计划明示需要的运维补口） |
| G22-3 状态机与不可变 | ✅ | DRAFT→IN_REVIEW→PUBLISHED→DEPRECATED；`canTransitionTo` 单向；PUT 非 DRAFT → 409（`duplicateCodeAndImmutablePublished`）；PUBLISHED 只能新建版本；发布自动取代旧发布版（事件留痕「被 vN 取代」） |
| G22-4 非法输入拒绝 | ✅ | 同码重复（409「标准代码已存在」）；版本倒退（导入携带 versionNo<1 拒绝；常规路径版本号服务端递增不可倒退）；非法类型（白名单 STRING/INTEGER/DECIMAL/DATE/DATETIME/BOOLEAN/CODE）；空值域（CODE ≥1 值）；另含非 CODE 携带值域拒绝、元素/值 code 重复拒绝——`invalidTypeAndEmptyDomainAndDuplicatesRejected` |
| G22-5 权限三级 | ✅ | 网关层：GET 六角色、POST publish/deprecate/sync 仅 platform/tenant-admin、其余写操作工程师及以上（`data-standards` 与 `data-standard-versions` 两个前缀都声明——单前缀罩不住另一路径段）；`StandardSecurityTest`：viewer 读 OK 写 403、工程师提交 OK 发布 403、管理员发布 200 |
| G22-6 导入 dry-run 先行 | ✅ | CSV 模板（最小 RFC4180 解析器，含引号转义）+ 内部 JSON 同构；dryRun=true 只出报告（elementCount/valueCount/problems）不落库，`importDryRunThenCommitWithCsv` 验证 dry-run 后计数 0、落库后 1、重复导入冲突进 problems |
| G22-7 OM 同步 SYNC_PENDING | ✅ | 发布后经 `OpenMetadataTermSyncClient`（复用血缘 OM 凭据，条件装配）投影词表术语；失败/未配置 → SYNC_PENDING + 事件，发布本身成功；人工重试端点闭环。`OpenMetadataTermSyncClientTest`（HttpServer 桩）：推送体含业务术语名与词表 id、409 同名幂等、词表缺失/网关失败明确报错。OM 不可用时标准本地事实完全可读（同步只是投影状态） |
| G22-8 FHIR 导出 | ✅ | Bundle(collection) 内 CODE 元素 → CodeSystem + ValueSet；标识只用 `urn:dataos:standard:{code}:{element}` 业务口径，测试断言不含内部 version uuid/standard uuid；状态映射 active/retired/draft |
| G22-9 门户接真实 API | ✅ | `DataStandardsLive`：列表（搜索/状态筛选/分页）、版本轨（生命周期动作：提交/发布/停用/新版本/重试投影）、草稿修改（元信息 + 元素集 JSON 整体替换）、导入区（dry-run 预检报告 → 确认导入）、版本对比、影响范围、FHIR 下载、审计事件流。真实模式不读 mock.ts；无 API 显示「控制面暂不可用」不回静态（mock-audit 新锁 + 无头浏览器验证）；演示构建保留显式样例 |
| G22-10 compare/impact | ✅ | compare：added/removed/changed（类型/必填/定义/敏感级别/值域增减），跨标准对比 400；impact：元素 asset_ref 聚合 + G23 映射面预留说明 |
| G22-11 矩阵 + gate 文档 | ✅（本地+CI 面） | 正常主链/非法流转/边界/权限/依赖故障（OM 未配置→SYNC_PENDING、API 断→诚实不可用）/幂等（版本号唯一约束、同名术语 409 幂等）/回滚（停用与新版本取代，事件不删） |
| C-3 隐私 | ✅ | 标准域无 PHI 面；FHIR/投影不含连接串或密钥 |

## 工程备注

- 越租户参数（`?tenantId=` 越权）此前会落 500：中央异常处理器新增 `AccessDeniedException → 403` 映射（全控制面受益）。
- 元素复制建新版本时主键必须全部重新生成（内容复制非行复制），否则唯一约束冲突——测试实抓。

## dev 真实链路证据（2026-09-21 实测，控制面 0.2.0-g22-g24-20260921 + 门户 dist 热更）

- **导入→发布全链**：CSV `STD-REG-CHANNEL 挂号渠道标准`（3 数据元/3 值域）dry-run 报告 problems=[] → 落库 DRAFT → submit IN_REVIEW → publish PUBLISHED（OM 词表未建时如实 SYNC_PENDING，事件链 CREATED→SUBMITTED→PUBLISHED→SYNC_PENDING）。
- **OM 术语投影闭环**：dev OM 建词表「数据标准」（201）→ `/sync` 重试 → **SYNCED**（事件 SYNC_SUCCEEDED）→ OM 词表内核验三条术语 `数据标准."STD-REG-CHANNEL.{channel,reg_fee,reg_time}"`；其中 channel 一条经 409 幂等路径收编（重试语义实测）。
- **实抓真缺陷一个**：OM 1.6 `CreateGlossaryTerm.glossary` 只收词表**名/fqn 字符串**——对象形式 400、裸 id 404「instance not found」；初版客户端发 `{id}`，dev 实测连败两轮后修正为词表名（本地 HttpServer 桩测试同步收紧断言），重建镜像重部署后一次通过。
- **防重与版本**：同码重复创建 409；基于已发布 v1 新建草稿 v2（元素复制）；v2 修改元素（含 asset_ref 绑定 ods_ep.ep_mz_cfzb）后 compare v2↔v1 输出 added=reg_time / changed=channel（定义变更 + 值域 +1/-0）；impact v2 引用资产 `doris-dataos.default.ods_ep.ep_mz_cfzb`。
- **FHIR 导出**：真实调用返回 Bundle，CodeSystem `urn:dataos:standard:STD-REG-CHANNEL:channel` 概念 OPD/ER/PHY，无内部 id。
- **门户（1440×900 无头 Chrome + DOM 证据）**：`/governance/standards` 渲染 STD-REG-CHANNEL（5 处）/挂号渠道（7 处）/已发布/术语投影状态；列表、版本轨、元素表、审计事件均真实数据。
