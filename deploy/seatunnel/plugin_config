# Only the connectors required by the first clinical batch slice plus the
# hospital-edge S3 relay source. Do not add Shell or a catch-all plugin to
# the production image.
--connectors-v2--
connector-jdbc
connector-doris
connector-file-s3
