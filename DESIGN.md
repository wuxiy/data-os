# 医数中枢 · 第一版原型设计系统

## 1. Visual Theme and Atmosphere

这是面向院领导、信息中心负责人、治理人员和实施工程师的“临床路径式治理工作台”。界面以可信、克制、可追责为主：温润灰米白画布、白色工作面、哑光深灰松石导航，不做装饰性大屏，不用通用卡片瀑布。

视觉记忆点是贯穿页面的“治理责任链”：异常指标不是终点，点击后沿“数据对象 → 规则 → 影响 → 责任部门 → 处理结果”展开，像临床路径一样让治理过程可解释、可行动。

## 2. Color Palette and Roles

| 名称 | HEX | CSS/OKLCH 角色 |
|---|---|---|
| 临床画布 | `#F4F5F1` | 温润灰米白；主工作区背景 |
| 纸面白 | `#FFFFFF` | `oklch(100% 0 0)`；表格、抽屉和高优先级工作面 |
| 浅灰绿 | `#ECEEE9` | 侧边导航底；与画布同族，靠色阶而非深块分隔 |
| 墨绿 | `#20312D` | 主文字、标题 |
| 苔灰 | `#687C75` | `oklch(56% 0.025 165)`；辅助文字、轴标签 |
| 灰玉绿 | `#2E7669` | 低饱和；主动作、健康状态、责任链进度 |
| 暗金灰 | `#C0A56A` | 只用于侧栏选中项的 2px 标记 |
| 药珀色 | `#B96C26` | `oklch(58% 0.13 60)`；风险、逾期和需关注状态 |
| 朱砂红 | `#B4483E` | `oklch(54% 0.14 28)`；失败、阻断和破坏性变更 |

色彩复核：删除蓝青/祖母绿渐变与高亮发光；侧栏使用浅灰绿融入画布，选中态只保留低饱和灰玉淡底和细暗金灰标记。医疗属性由秩序、低色度与纸面层级表达，不使用盾牌、十字、心电图等表面符号。

## 3. Typography Rules

- 页面标题：`"Noto Serif SC", "Songti SC", serif`，只用于 24–32px 标题，600，行高 1.2，字距 `-0.012em`。使用宋体结构表达制度、标准和档案感。
- 正文与控件：`"Noto Sans SC", "PingFang SC", sans-serif`，12–16px，400/500/600，行高 1.5。
- 指标与运行标识：`"DIN Alternate", "Roboto Mono", monospace`，12–30px，使用等宽数字。
- 所有按钮、标签、表格列名使用句式大小写，不用营销式标题。

## 4. Component Stylings

- 按钮：固定 7px 圆角；主按钮玉石绿底，次按钮白底加细分隔线；按下 `scale(0.96)`，150ms ease-out。
- 导航：浅灰绿侧栏，选中项使用灰玉淡底（`rgba(46,118,105,0.12)`）与墨色文字，仅允许 2px 暗金灰竖线。
- 状态：只在状态确有语义时使用小型圆角标签；健康/风险/阻断分别为绿/珀/红。
- 表格：白色工作面、48px 行高、细分隔线；文字左对齐、数字右对齐；悬停只改变背景色。
- 图表：低对比网格线，玉石绿主线、药珀色风险线；不用超过四块的饼图。
- 抽屉：右侧责任链详情，12px 圆角，仅使用两级背景和柔和阴影，不用模态弹窗；标题下必须展示链路编号、关联键和刷新时间，每个节点必须注明来源。

## 5. Layout Principles

- 桌面端：232px 固定侧栏 + 流式主区；主区最大宽度 1480px，24px 外边距。
- 页面遵循“定位 → 状态 → 行动”：标题/范围栏 → 状态带 → 主工作区 → 责任链抽屉。
- 间距刻度：`4 / 8 / 12 / 16 / 24 / 32 / 48`。
- 圆角刻度：`4 / 5 / 7 / 8 / 10 / pill`；面板 10px、控件 7px；pill 仅用于状态和筛选，不用于普通按钮。
- CSS 策略：CSS Modules；仅全局保存 reset、字体和设计令牌，不混用 Tailwind/CSS-in-JS。

```text
┌─────────────┬─────────────────────────────────────────────────────┐
│ 医数中枢     │ 页面标题 / 机构范围 / 截至时间                         │
│             ├─────────────────────────────────────────────────────┤
│ 首页         │ 今日治理状态带：关键结果 + 一条可点击责任链              │
│ 数据接入     ├──────────────────────────┬──────────────────────────┤
│ 数据资产     │ 主视图：趋势、矩阵或表格     │ 风险排名 / 当前待办          │
│ 数据治理     │                          │                          │
│ 主索引       ├──────────────────────────┴──────────────────────────┤
│ 数据服务     │ 问题列表、版本轨迹或证据                              │
└─────────────┴─────────────────────────────────────────────────────┘
```

## 6. Depth and Elevation

- Level 0：临床画布 `#F4F5F1`。
- Level 1：白色工作面，用背景明度差区分，不加阴影。
- Level 2：浮动筛选和抽屉，`0 8px 28px rgba(20,44,39,.12)`。
- 分隔优先使用 1px `rgba(20,44,39,.10)`；不为每块内容创建卡片。

## 7. Do's and Don'ts

- 做：指标必须显示口径、截至时间、目标和下钻入口。
- 做：每个异常最终都能定位责任对象、责任人和动作。
- 做：责任链必须逐项说明事件、资产、规则、责任人与工单来源。
- 做：使用医院、系统、主题域和真实中文治理语汇。
- 做：不同页面使用表格、矩阵、轨迹和双栏比对等不同结构。
- 不做：轮播、地球、粒子、霓虹、玻璃拟态和紫蓝渐变。
- 不做：四张同构指标卡后再堆多张同构卡片。
- 不做：把 OpenMetadata、dbt、SeaTunnel 等引擎名暴露给业务用户。
- 不做：无意义徽章、营销文案或无来源的整数指标。

## 8. Desktop Scope

- 首版发布目标为 1280–1920px 医院桌面工作站；1440×900 作为设计和截图基准。
- 1280px 保持完整业务链路与可操作性，复杂表格允许工作区内横向滚动，不允许页面整体横向溢出。
- 已有窄屏自适应样式仅作为基础容错，不纳入首版验收，也不继续扩展移动端专属交互。
- 桌面端支持键盘焦点、跳转主内容链接和 `prefers-reduced-motion`。

## 9. Agent Prompt Guide

- 驾驶舱：在 `#F4F5F1` 画布上创建 232px 无渐变 `#ECEEE9` 侧栏与白色工作面；标题 26px Noto Serif SC 600；主动作 `#2E7669`、7px 圆角；状态带使用开放布局而不是卡片网格。
- 数据表：48px 行高、1px `rgba(20,44,39,.10)` 分隔、正文 14px Noto Sans SC、数字 DIN Alternate 右对齐、悬停背景 `#F1F6F3`。
- 责任链抽屉：宽 420px、白底、12px 圆角、阴影 `0 8px 28px rgba(20,44,39,.12)`；五个阶段用 2px 玉石绿连线，风险节点用 `#B96C26`。
- 主按钮：高 40px、左右 16px、6px 圆角、`#2E7669` 背景、白字 14px/600；按下缩放至 0.96，150ms ease-out。

## 10. Release-quality Gates

- 页面必须覆盖默认、悬停、焦点、禁用、成功和异常状态；所有可点击元素必须有可见反馈。
- 关键流程在 1440×900 与 1280×800 桌面视口可完成，不允许页面整体横向溢出。
- 颜色不是唯一的状态表达方式；状态同时显示文字或图标。
- 业务文案使用真实医院治理术语，不出现 lorem、无含义占位数或暴露底层引擎名。
- 生产构建、键盘焦点、抽屉关闭、移动导航和关键数据操作均通过后才能交付。
