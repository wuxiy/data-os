spring:
  datasource:
    url: jdbc:h2:mem:dataos;MODE=PostgreSQL
    username: sa
    password:
  sql:
    init:
      mode: always

data-os:
  seed-demo: false
  runs:
    sync-interval-ms: 3600000
    sync-initial-delay-ms: 3600000
