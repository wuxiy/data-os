spring:
  datasource:
    url: jdbc:h2:mem:mpi;MODE=PostgreSQL
    username: sa
    password:
  sql:
    init:
      mode: never
  flyway:
    enabled: true
    locations: classpath:db/migration
    schemas: data_os_mpi

data-os:
  auth:
    mode: DISABLED
